package com.gielinorspeaks.model;

/**
 * Represents the source of a dialogue event.
 */
public enum DialogueSource {
	/**
	 * Traditional NPC conversation interface (dialogue box)
	 */
	DIALOGUE_BOX,

	/**
	 * Text appearing above NPC's head
	 */
	OVERHEAD_TEXT
}
